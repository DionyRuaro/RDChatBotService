package com.rd.repository;

import com.rd.domain.MensagemBot;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MensagemBotRepository extends JpaRepository<MensagemBot, Long> {
}
